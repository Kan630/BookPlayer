<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">

            <!-- Radio deep link intent filters (full variant only) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="bookplayer.driot.com"
                    android:pathPrefix="/share/radio" />
            </intent-filter>

            <!-- Custom URI schemes for radio deep links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bookplayerfull" android:host="radio" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bookplayer" android:host="radio" />
            </intent-filter>

            <!-- Voice search ("Hey Google, play <query> on BookPlayer") launched directly as an
                 activity intent (e.g. from the phone's Assistant when the app isn't already the
                 active media session) - see MainActivity.handleMediaSearchIntentIfAny() and the
                 MediaSessionCompat.Callback.onPlayFromSearch() path in MediaService, which covers
                 the same voice command when a session is already connected (Android Auto). -->
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>
    </application>

</manifest>
