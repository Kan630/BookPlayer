<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path name="unzipped" path="files/unzipped/"/>

    <external-path
        name="external"
        path="." />
    <external-files-path
        name="external_files"
        path="." />
    <cache-path
        name="cache"
        path="." />
    <external-cache-path
        name="external_cache"
        path="." />
    <files-path
        name="files"
        path="." />
    <external-path
        name="external_downloads"
        path="Download/"
        />

    <!-- Covers any mounted volume (secondary/SD card included), not just primary external
         storage - needed so FileProvider.getUriForFile() can grant a content:// Uri for a file
         living on a removable SD card (e.g. instrumented-test fixtures read directly from
         <sd-card-root>/fixtures/, see LoadManyBookTest). -->
    <root-path
        name="root"
        path="." />

</paths>
