<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/colorPrimary"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="4dp"
    android:paddingBottom="0dp"
    android:gravity="center_vertical"
    android:elevation="8dp"
    >

    <!-- Row 1: cover + title/subtitle + play/pause + close -->
    <LinearLayout
        android:id="@+id/miniControlsRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/miniControlsRow2"
        >

        <ImageView
            android:id="@+id/ivCover"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:scaleType="centerInside"
            android:contentDescription="@string/cover_preview"
            android:src="@drawable/ic_audio_file_24px"
            android:layout_marginEnd="8dp"
            />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_weight="1">

            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:singleLine="true"
                android:ellipsize="middle"
                android:textColor="?attr/colorOnPrimary"
                android:textStyle="bold"
                />

            <TextView
                android:id="@+id/tvSubTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:singleLine="true"
                android:ellipsize="end"
                android:textSize="12sp"
                android:alpha="0.8"
                android:textColor="?attr/colorOnPrimary"
                />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/progress"
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginHorizontal="8dp"
            android:visibility="gone"
            />

        <ImageButton
            android:id="@+id/bMiniBackward"
            android:src="@drawable/ic_media_fast_rewind_24"
            style="@style/miniPlayerButton"
            android:layout_marginHorizontal="4dp"
            />

        <ImageButton
            android:id="@+id/bMiniPlayPause"
            android:src="@drawable/ic_media_play_24"
            style="@style/miniPlayerButton"
            android:layout_marginHorizontal="4dp"
            />

        <ImageButton
            android:id="@+id/bMiniForward"
            android:src="@drawable/ic_media_fast_forward_24"
            style="@style/miniPlayerButton"
            android:layout_marginHorizontal="4dp"
            />

        <ImageButton
            android:id="@+id/ibClose"
            android:src="@drawable/ic_media_close_24"
            style="@style/miniPlayerButton"
            android:layout_marginStart="8dp"
            />
    </LinearLayout>

    <!-- Row 2: seek bar + time -->
    <LinearLayout
        android:id="@+id/miniControlsRow2"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintTop_toBottomOf="@id/miniControlsRow"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        >
        <com.google.android.material.slider.Slider
            android:id="@+id/sbMiniSeek"
            android:layout_width="0dp"
            android:layout_height="10dp"
            android:layout_weight="1"
            app:labelBehavior="floating"
            app:trackHeight="2dp"
            app:trackColor="?attr/colorSecondary"
            app:thumbColor="?attr/colorSecondary"
            />
        <TextView
            android:id="@+id/tvMiniTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?attr/colorOnSecondary"
            android:textSize="12sp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="5dp"
            android:text="--:--/--:--"
            />
    </LinearLayout>

    <!-- Tiny "Preview" badge (floats top-end) -->
    <TextView
        android:id="@+id/labelPreview"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/preview_badge"
        android:textStyle="italic"
        android:textSize="10sp"
        android:textColor="?attr/colorOnSecondary"
        android:alpha="0.8"
        android:paddingEnd="2dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
